{
  "name": "moire-magic",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "parcel index.html -p 8000",
    "build": "rm -rf docs/* && parcel build --out-dir docs --public-url ./ --no-source-maps index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Chris Chang <c@crccheck.com> (http://crccheck.com/blog)",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^4.1.10",
    "sass": "^1.20.1"
  }
}
